{
  "name": "kablonet",
  "version": "1.0.0",
  "description": "Kablonet CLI",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive test",
    "coverage": "nyc --reporter=text mocha --recursive test",
    "coverage-html": "nyc --reporter=html mocha --recursive test",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "snyk-protect": "snyk protect",
    "prepare": "npm run snyk-protect"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:tosbaha/kablonet.git"
  },
  "keywords": [
    "kablonet",
    "turksat",
    "kota"
  ],
  "author": "Mustafa Dur",
  "license": "MIT",
  "bin": {
    "kablonet": "./bin/kablonet"
  },
  "engines": {
    "node": ">=8"
  },
  "dependencies": {
    "cli-table3": "^0.5.1",
    "commander": "^2.14.1",
    "configstore": "^3.1.1",
    "inquirer": "^5.1.0",
    "ora": "^2.0.0",
    "request": "^2.87.0",
    "request-promise": "^4.2.2",
    "snyk": "^1.88.0"
  },
  "devDependencies": {
    "child_process": "^1.0.2",
    "codecov": "^3.0.4",
    "dotenv": "^5.0.0",
    "mocha": "^5.0.1",
    "nyc": "^12.0.2",
    "should": "^13.2.1",
    "yakbak": "^3.0.0"
  },
  "snyk": true
}
